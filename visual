Treat the design system established in Screen0Mode.tsx as the baseline
for all subsequent screens.

Now implement Screen 2 using:
./design-reference/screen-02-source-target.png

Preserve the same:
- sidebar
- header
- stepper
- typography
- spacing
- card styling
- buttons
- colors
- borders
- responsive behavior

Do not redesign the global shell independently.
Implement only the Screen 2 content area.
