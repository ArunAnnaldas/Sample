Screen 2 is approved and complete.

Treat the existing implementation of Screen 1 + Screen 2 as the visual and structural baseline for the remaining screens.

Now implement ONLY Screen 3: Column Mapping.

Primary implementation target:
src/components/screens/Screen2ColumnSelector.tsx

Use the attached Screen 3 reference image and UI_REDESIGN.md as guidance.

Important constraints:
- Do NOT modify Screen0ReconMode.tsx.
- Do NOT modify Screen1FileUpload.tsx / Screen1DbSetup.tsx / Screen1SourceTypeSelector.tsx unless absolutely necessary.
- Do NOT redesign the existing sidebar, header, stepper, typography, spacing, button styles, or page shell.
- Reuse the design system already established in Screens 1 and 2.
- Preserve all Zustand state, mapping logic, API behavior, validation, navigation and existing business rules.
- No backend/API changes.

SCREEN 3 OBJECTIVE:
Make column mapping easy to scan, review and adjust.

Required UI:
1. Page heading:
   "Map Columns"

2. Supporting text:
   "Review automatically suggested mappings and adjust where required."

3. Summary metrics:
   - Mapped
   - Unmapped
   - Match %
   Use only values already available from existing state/data.

4. Column mapping area:
   Clearly show:
   Source Column → Target Column

   Include:
   - source column name
   - source datatype where available
   - target mapping
   - target datatype where available
   - match/confidence score where already available
   - comparison type
   - selection state

5. Match confidence presentation:
   - 100% / exact
   - high confidence
   - partial
   - unmapped

   Use semantic color carefully and always show textual/percentage meaning.

6. Preserve all existing mapping capabilities:
   - automatic mappings
   - manual target selection
   - selected/unselected mappings
   - exact comparison
   - tolerance
   - fuzzy comparison
   - case-sensitive option
   - any current validation

7. If practical without backend changes, provide compact client-side filters:
   - All
   - Selected
   - Needs Review
   - Unmapped

8. Keep technical field names readable.
   Monospace styling is acceptable for field names.

9. Bottom actions:
   - Back
   - Auto Map, if currently supported
   - Next: Match Rules

10. Next must respect the current validation/business rules.

UX REQUIREMENTS:
- Avoid excessive vertical scrolling where practical.
- Do not create giant cards for every mapping.
- Optimize for enterprise desktop use and large mapping sets.
- Make mappings visually scannable.
- Preserve accessibility and keyboard usage.
- Do not invent confidence scores or backend data.

Before editing:
1. Summarize current Screen2ColumnSelector.tsx behavior.
2. Identify state/API dependencies.
3. List exact files to modify.
4. Explain how existing functionality will be preserved.

After implementation:
1. Run TypeScript/type-check.
2. Run frontend build.
3. Fix errors introduced by the change.
4. Report files changed.
5. Explicitly state whether any business logic, Zustand behavior or API behavior changed.
6. STOP after Screen 3.
